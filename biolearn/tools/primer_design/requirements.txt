# Primer Design Tool - Requirements
# Core dependencies (Python standard library only for basic functionality)

# Optional: For enhanced alignment (recommended)
# Install one of these alignment tools:
# - MAFFT: conda install -c bioconda mafft
# - MUSCLE: conda install -c bioconda muscle
# - ClustalW: conda install -c bioconda clustalw

# Optional: For advanced features
# biopython>=1.79  # Alternative sequence handling
# requests>=2.28   # Alternative HTTP client

# Note: This tool is designed to work with Python standard library only
# to minimize dependencies. External alignment tools (MAFFT, MUSCLE)
# are optional but recommended for better alignment quality.
